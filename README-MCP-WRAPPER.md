# One-shot wrapper documentation

See [Use the wrapper](docs/use/wrapper.md) for the one-shot JSON action interface
and [MCP reference](docs/reference/mcp.md) for its relationship to the MCP server.

The existing `mcp-wrapper.py` entry point remains supported. Request and response
examples remain in [examples/](examples/); schemas remain in [schemas/](schemas/).

To connect an MCP client, start with [Use MCP](docs/use/mcp.md).
